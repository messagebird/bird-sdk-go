// Code generated by surface-gen; DO NOT EDIT.
package bird

import (
	"context"
	"net/http"
	"time"

	"github.com/messagebird/bird-sdk-go/internal/oapi"
	"github.com/messagebird/bird-sdk-go/option"
)

// WhatsappStatsSummaryParams filters the summary read.
type WhatsappStatsSummaryParams struct {
	// Inclusive start of the window: a calendar day (YYYY-MM-DD) or an RFC 3339 instant rounded down to the hour. The `timezone` parameter makes a calendar day local and rounds an instant down to the local hour. Omit `timezone` to use UTC. When `timezone` is set, a numeric UTC offset such as `+05:45` is rejected; use a calendar day or a `Z` (UTC) instant. This value must use the same form as `to`. When omitted, it defaults to 30 days before `to` for day windows or 168 hours (7 days) before `to` for hour windows.
	From string
	// Inclusive end of the window: a calendar day (YYYY-MM-DD) or an RFC 3339 instant rounded down to the hour. The `timezone` parameter makes a calendar day local and rounds an instant down to the local hour. Omit `timezone` to use UTC. When `timezone` is set, a numeric UTC offset is rejected; use a calendar day or a `Z` (UTC) instant. This value must use the same form as `from`. When omitted, it defaults to today for day windows or the current hour for hour windows in that timezone. Day windows may not exceed 365 days; hour windows may not exceed 720 hours (30 days).
	To string
	// IANA timezone identifier used to group statistics, for example `Asia/Kathmandu`. The default is UTC. Day and hour boundaries, including the default window when `from` and `to` are omitted, follow this timezone. When this parameter is set, pass `from` and `to` as calendar days or `Z` instants instead of timestamps with explicit UTC offsets.
	Timezone string
	// Restricts the statistics to one template, identified by its ID (`wat_…`) or slug. Mutually exclusive with the other dimension filters (`category`, `phone_number`, `tag`); only one may be set per request. An ID matches the `template_id` key on a row of the per-template breakdown; a slug is accepted for callers that predate that key and resolves to the same messages.
	Template string
	// Restrict the statistics to a single template category. Mutually exclusive with the other dimension filters (`template`, `phone_number`, `tag`); only one may be set per request. Matches the `category` key on a row of the per-category breakdown.
	Category WhatsAppTemplateCategory
	// Restrict the statistics to a single business sender phone number, in E.164 form. Mutually exclusive with the other dimension filters (`template`, `category`, `tag`); only one may be set per request. Matches the `phone_number` key on a row of the per-phone-number breakdown.
	PhoneNumber string
	// Restrict the statistics to a single tag. Use `name` to match any value of a tag, or `name:value` for a specific pair (for example `campaign:spring_launch`). Mutually exclusive with the other dimension filters (`template`, `category`, `phone_number`); only one may be set per request. A row of the per-tag breakdown carries the same pair in its single `tag` key.
	Tag string
	// Set to `previous_period` to also include the same statistics for the immediately preceding window of equal length, plus the change between the two, so you can show "+X% vs last period" without a second request. The comparison window carries any dimension filter set on the request, so a filtered comparison compares like with like.
	Compare StatsComparePeriod
}

func (p WhatsappStatsSummaryParams) toWire() *oapi.GetWhatsAppStatsSummaryParams {
	return &oapi.GetWhatsAppStatsSummaryParams{
		From:        optStr(p.From),
		To:          optStr(p.To),
		Timezone:    optStr(p.Timezone),
		Template:    optStr(p.Template),
		Category:    optZero(p.Category),
		PhoneNumber: optStr(p.PhoneNumber),
		Tag:         optStr(p.Tag),
		Compare:     optZero(p.Compare),
	}
}

// WhatsappStatsDailyParams filters the daily read.
type WhatsappStatsDailyParams struct {
	// Start date (inclusive), YYYY-MM-DD. Interpreted as a calendar day in `timezone` (a UTC day when `timezone` is omitted). The window may not exceed 365 days. Defaults to 30 days before `to` when omitted.
	From time.Time
	// End date (inclusive), YYYY-MM-DD. Interpreted as a calendar day in `timezone` (a UTC day when `timezone` is omitted). The window may not exceed 365 days. Defaults to today in that timezone when omitted.
	To time.Time
	// IANA timezone identifier used to group statistics, for example `Asia/Kathmandu`. The default is UTC. Day and hour boundaries, including the default window when `from` and `to` are omitted, follow this timezone. When this parameter is set, pass `from` and `to` as calendar days or `Z` instants instead of timestamps with explicit UTC offsets.
	Timezone string
	// Restricts the statistics to one template, identified by its ID (`wat_…`) or slug. Mutually exclusive with the other dimension filters (`category`, `phone_number`, `tag`); only one may be set per request. An ID matches the `template_id` key on a row of the per-template breakdown; a slug is accepted for callers that predate that key and resolves to the same messages.
	Template string
	// Restrict the statistics to a single template category. Mutually exclusive with the other dimension filters (`template`, `phone_number`, `tag`); only one may be set per request. Matches the `category` key on a row of the per-category breakdown.
	Category WhatsAppTemplateCategory
	// Restrict the statistics to a single business sender phone number, in E.164 form. Mutually exclusive with the other dimension filters (`template`, `category`, `tag`); only one may be set per request. Matches the `phone_number` key on a row of the per-phone-number breakdown.
	PhoneNumber string
	// Restrict the statistics to a single tag. Use `name` to match any value of a tag, or `name:value` for a specific pair (for example `campaign:spring_launch`). Mutually exclusive with the other dimension filters (`template`, `category`, `phone_number`); only one may be set per request. A row of the per-tag breakdown carries the same pair in its single `tag` key.
	Tag string
}

func (p WhatsappStatsDailyParams) toWire() *oapi.GetWhatsAppStatsDailyParams {
	return &oapi.GetWhatsAppStatsDailyParams{
		From:        optDate(p.From),
		To:          optDate(p.To),
		Timezone:    optStr(p.Timezone),
		Template:    optStr(p.Template),
		Category:    optZero(p.Category),
		PhoneNumber: optStr(p.PhoneNumber),
		Tag:         optStr(p.Tag),
	}
}

// WhatsappStatsHourlyParams filters the hourly read.
type WhatsappStatsHourlyParams struct {
	// Start of the window (RFC 3339 instant), rounded down to the start of its hour and included. The boundary uses the local hour when `timezone` is set and the UTC hour otherwise. The window may not exceed 30 days (720 hours). Defaults to 168 hours (7 days) before `to` when omitted.
	From time.Time
	// End of the window (RFC 3339 instant), rounded down to the start of its hour and included. The boundary uses the local hour when `timezone` is set and the UTC hour otherwise, so both bounds are inclusive. The window may not exceed 30 days (720 hours). Defaults to the current hour when omitted.
	To time.Time
	// IANA timezone identifier used to group statistics, for example `Asia/Kathmandu`. The default is UTC. Day and hour boundaries, including the default window when `from` and `to` are omitted, follow this timezone. When this parameter is set, pass `from` and `to` as calendar days or `Z` instants instead of timestamps with explicit UTC offsets.
	Timezone string
	// Restricts the statistics to one template, identified by its ID (`wat_…`) or slug. Mutually exclusive with the other dimension filters (`category`, `phone_number`, `tag`); only one may be set per request. An ID matches the `template_id` key on a row of the per-template breakdown; a slug is accepted for callers that predate that key and resolves to the same messages.
	Template string
	// Restrict the statistics to a single template category. Mutually exclusive with the other dimension filters (`template`, `phone_number`, `tag`); only one may be set per request. Matches the `category` key on a row of the per-category breakdown.
	Category WhatsAppTemplateCategory
	// Restrict the statistics to a single business sender phone number, in E.164 form. Mutually exclusive with the other dimension filters (`template`, `category`, `tag`); only one may be set per request. Matches the `phone_number` key on a row of the per-phone-number breakdown.
	PhoneNumber string
	// Restrict the statistics to a single tag. Use `name` to match any value of a tag, or `name:value` for a specific pair (for example `campaign:spring_launch`). Mutually exclusive with the other dimension filters (`template`, `category`, `phone_number`); only one may be set per request. A row of the per-tag breakdown carries the same pair in its single `tag` key.
	Tag string
}

func (p WhatsappStatsHourlyParams) toWire() *oapi.GetWhatsAppStatsHourlyParams {
	return &oapi.GetWhatsAppStatsHourlyParams{
		From:        optTime(p.From),
		To:          optTime(p.To),
		Timezone:    optStr(p.Timezone),
		Template:    optStr(p.Template),
		Category:    optZero(p.Category),
		PhoneNumber: optStr(p.PhoneNumber),
		Tag:         optStr(p.Tag),
	}
}

// WhatsappStatsByErrorCodeParams filters the by_error_code read.
type WhatsappStatsByErrorCodeParams struct {
	// Inclusive start of the window, a calendar day (YYYY-MM-DD). Interpreted in `timezone`, or UTC when omitted. Must not be after `to`. Max window 365 days. Defaults to 30 days before `to` when omitted.
	From time.Time
	// Inclusive end of the window, a calendar day (YYYY-MM-DD). Interpreted in `timezone`, or UTC when omitted. Max window 365 days. Defaults to today in that timezone when omitted.
	To time.Time
	// IANA timezone identifier used to group statistics, for example `Asia/Kathmandu`. The default is UTC. Day and hour boundaries, including the default window when `from` and `to` are omitted, follow this timezone. When this parameter is set, pass `from` and `to` as calendar days or `Z` instants instead of timestamps with explicit UTC offsets.
	Timezone string
	// Maximum number of error-code rows to return, ranked by failure count descending.
	Limit int
}

func (p WhatsappStatsByErrorCodeParams) toWire() *oapi.GetWhatsAppStatsByErrorCodeParams {
	return &oapi.GetWhatsAppStatsByErrorCodeParams{
		From:     optDate(p.From),
		To:       optDate(p.To),
		Timezone: optStr(p.Timezone),
		Limit:    optInt(p.Limit),
	}
}

// WhatsappStatsByTemplateParams filters the by_template read.
type WhatsappStatsByTemplateParams struct {
	// Inclusive start of the window, a calendar day (YYYY-MM-DD). Interpreted in `timezone`, or UTC when omitted. Must not be after `to`. Max window 365 days. Defaults to 30 days before `to` when omitted.
	From time.Time
	// Inclusive end of the window, a calendar day (YYYY-MM-DD). Interpreted in `timezone`, or UTC when omitted. Max window 365 days. Defaults to today in that timezone when omitted.
	To time.Time
	// IANA timezone identifier used to group statistics, for example `Asia/Kathmandu`. The default is UTC. Day and hour boundaries, including the default window when `from` and `to` are omitted, follow this timezone. When this parameter is set, pass `from` and `to` as calendar days or `Z` instants instead of timestamps with explicit UTC offsets.
	Timezone string
	// Maximum number of template rows to return, ranked by accepted volume descending.
	Limit int
}

func (p WhatsappStatsByTemplateParams) toWire() *oapi.GetWhatsAppStatsByTemplateParams {
	return &oapi.GetWhatsAppStatsByTemplateParams{
		From:     optDate(p.From),
		To:       optDate(p.To),
		Timezone: optStr(p.Timezone),
		Limit:    optInt(p.Limit),
	}
}

// WhatsappStatsByTemplateCategoryParams filters the by_template_category read.
type WhatsappStatsByTemplateCategoryParams struct {
	// Inclusive start of the window, a calendar day (YYYY-MM-DD). Interpreted in `timezone`, or UTC when omitted. Must not be after `to`. Max window 365 days. Defaults to 30 days before `to` when omitted.
	From time.Time
	// Inclusive end of the window, a calendar day (YYYY-MM-DD). Interpreted in `timezone`, or UTC when omitted. Max window 365 days. Defaults to today in that timezone when omitted.
	To time.Time
	// IANA timezone identifier used to group statistics, for example `Asia/Kathmandu`. The default is UTC. Day and hour boundaries, including the default window when `from` and `to` are omitted, follow this timezone. When this parameter is set, pass `from` and `to` as calendar days or `Z` instants instead of timestamps with explicit UTC offsets.
	Timezone string
	// Maximum number of template-category rows to return, ranked by accepted volume descending.
	Limit int
}

func (p WhatsappStatsByTemplateCategoryParams) toWire() *oapi.GetWhatsAppStatsByTemplateCategoryParams {
	return &oapi.GetWhatsAppStatsByTemplateCategoryParams{
		From:     optDate(p.From),
		To:       optDate(p.To),
		Timezone: optStr(p.Timezone),
		Limit:    optInt(p.Limit),
	}
}

// WhatsappStatsByTagParams filters the by_tag read.
type WhatsappStatsByTagParams struct {
	// Inclusive start of the window, a calendar day (YYYY-MM-DD). Interpreted in `timezone`, or UTC when omitted. Must not be after `to`. Max window 365 days. Defaults to 30 days before `to` when omitted.
	From time.Time
	// Inclusive end of the window, a calendar day (YYYY-MM-DD). Interpreted in `timezone`, or UTC when omitted. Max window 365 days. Defaults to today in that timezone when omitted.
	To time.Time
	// IANA timezone identifier used to group statistics, for example `Asia/Kathmandu`. The default is UTC. Day and hour boundaries, including the default window when `from` and `to` are omitted, follow this timezone. When this parameter is set, pass `from` and `to` as calendar days or `Z` instants instead of timestamps with explicit UTC offsets.
	Timezone string
	// Maximum number of tag rows to return, ranked by accepted volume descending.
	Limit int
}

func (p WhatsappStatsByTagParams) toWire() *oapi.GetWhatsAppStatsByTagParams {
	return &oapi.GetWhatsAppStatsByTagParams{
		From:     optDate(p.From),
		To:       optDate(p.To),
		Timezone: optStr(p.Timezone),
		Limit:    optInt(p.Limit),
	}
}

// WhatsappStatsByPhoneNumberParams filters the by_phone_number read.
type WhatsappStatsByPhoneNumberParams struct {
	// Inclusive start of the window, a calendar day (YYYY-MM-DD). Interpreted in `timezone`, or UTC when omitted. Must not be after `to`. Max window 365 days. Defaults to 30 days before `to` when omitted.
	From time.Time
	// Inclusive end of the window, a calendar day (YYYY-MM-DD). Interpreted in `timezone`, or UTC when omitted. Max window 365 days. Defaults to today in that timezone when omitted.
	To time.Time
	// IANA timezone identifier used to group statistics, for example `Asia/Kathmandu`. The default is UTC. Day and hour boundaries, including the default window when `from` and `to` are omitted, follow this timezone. When this parameter is set, pass `from` and `to` as calendar days or `Z` instants instead of timestamps with explicit UTC offsets.
	Timezone string
	// Maximum number of phone-number rows to return, ranked by accepted volume descending.
	Limit int
}

func (p WhatsappStatsByPhoneNumberParams) toWire() *oapi.GetWhatsAppStatsByPhoneNumberParams {
	return &oapi.GetWhatsAppStatsByPhoneNumberParams{
		From:     optDate(p.From),
		To:       optDate(p.To),
		Timezone: optStr(p.Timezone),
		Limit:    optInt(p.Limit),
	}
}

// WhatsappStatsByCountryParams filters the by_country read.
type WhatsappStatsByCountryParams struct {
	// Inclusive start of the window, a calendar day (YYYY-MM-DD). Interpreted in `timezone`, or UTC when omitted. Must not be after `to`. Max window 365 days. Defaults to 30 days before `to` when omitted.
	From time.Time
	// Inclusive end of the window, a calendar day (YYYY-MM-DD). Interpreted in `timezone`, or UTC when omitted. Max window 365 days. Defaults to today in that timezone when omitted.
	To time.Time
	// IANA timezone identifier used to group statistics, for example `Asia/Kathmandu`. The default is UTC. Day and hour boundaries, including the default window when `from` and `to` are omitted, follow this timezone. When this parameter is set, pass `from` and `to` as calendar days or `Z` instants instead of timestamps with explicit UTC offsets.
	Timezone string
	// Maximum number of country rows to return, ranked by accepted volume descending.
	Limit int
}

func (p WhatsappStatsByCountryParams) toWire() *oapi.GetWhatsAppStatsByCountryParams {
	return &oapi.GetWhatsAppStatsByCountryParams{
		From:     optDate(p.From),
		To:       optDate(p.To),
		Timezone: optStr(p.Timezone),
		Limit:    optInt(p.Limit),
	}
}

// Summary Aggregate outbound WhatsApp counts for one window: accepted, sent, delivered, failed and rejected, with delivery and failure rates, read engagement, and processing and end-to-end latency percentiles. Restrict it to one dimension with `template`, `category`, `phone_number` or `tag`, one at a time, not combined. Set `compare` to `previous_period` for the deltas against the window immediately before. `from` and `to` are optional and default to the trailing 30 days. Counts are attributed to the time each message was accepted, so a delivery confirmed today counts against the day its message was accepted; `period.data_as_of` is the freshness boundary the rollups have reached.
func (s *WhatsappStatsService) Summary(ctx context.Context, params WhatsappStatsSummaryParams, opts ...option.RequestOption) (*WhatsAppStatsSummary, error) {
	body, err := s.get(ctx, opts, func(ctx context.Context, cfg requestConfig) (*http.Response, error) {
		return s.client.oapi.GetWhatsAppStatsSummary(ctx, params.toWire(), cfg...)
	})
	if err != nil {
		return nil, err
	}
	var out WhatsAppStatsSummary
	if err := decodeBody(body, &out); err != nil {
		return nil, err
	}
	return &out, nil
}

// Daily Outbound WhatsApp lifecycle counts, one row per calendar day of the window, gap-filled so a silent day is a zero row rather than a missing one. Every event a message produces counts against the day that message was accepted, so a row is a funnel over that day's sends and recent rows under-report while callbacks arrive. Each row carries its own latency percentiles, but delivery, failure and read rates are whole-window figures: read those from `whatsapp.stats.summary`. Accepts the same single-dimension restriction: `template`, `category`, `phone_number` or `tag`. Days are calendar days in `timezone`, or UTC when it is omitted. Max window 365 days; `from` and `to` default to the trailing 30 days.
func (s *WhatsappStatsService) Daily(ctx context.Context, params WhatsappStatsDailyParams, opts ...option.RequestOption) (*WhatsAppStatsResponse, error) {
	body, err := s.get(ctx, opts, func(ctx context.Context, cfg requestConfig) (*http.Response, error) {
		return s.client.oapi.GetWhatsAppStatsDaily(ctx, params.toWire(), cfg...)
	})
	if err != nil {
		return nil, err
	}
	var out WhatsAppStatsResponse
	if err := decodeBody(body, &out); err != nil {
		return nil, err
	}
	return &out, nil
}

// Hourly Outbound WhatsApp lifecycle counts, one row per hour, gap-filled. Every event a message produces counts against the hour that message was accepted, so a row is a funnel over that hour's sends and recent rows under-report while callbacks arrive. Each row carries its own latency percentiles, but delivery, failure and read rates are whole-window figures: read those from `whatsapp.stats.summary`. Use it where the daily grain hides the shape; a window spans at most 720 hours (30 days), and for anything longer use `whatsapp.stats.daily`. Accepts the same single-dimension restriction: `template`, `category`, `phone_number` or `tag`. `from` and `to` are optional RFC 3339 instants, defaulting to the trailing 168 hours.
func (s *WhatsappStatsService) Hourly(ctx context.Context, params WhatsappStatsHourlyParams, opts ...option.RequestOption) (*WhatsAppStatsResponse, error) {
	body, err := s.get(ctx, opts, func(ctx context.Context, cfg requestConfig) (*http.Response, error) {
		return s.client.oapi.GetWhatsAppStatsHourly(ctx, params.toWire(), cfg...)
	})
	if err != nil {
		return nil, err
	}
	var out WhatsAppStatsResponse
	if err := decodeBody(body, &out); err != nil {
		return nil, err
	}
	return &out, nil
}

// ByErrorCode Failed outbound WhatsApp messages grouped by the normalized failure reason, the same value a failed message reports in `last_error.code`, ranked by volume. This is the read that answers "why are sends failing"; `whatsapp.stats.summary` gives the failure rate, this gives its composition. A send declined before handoff (insufficient balance, missing price, suppressed recipient) counts as rejected rather than failed and is absent here; read `delivery.rejected` on `whatsapp.stats.summary` for it. Rows are the whole workspace's: the breakdown takes no dimension filter. Cap the rows with `limit` (default 50, max 200).
func (s *WhatsappStatsService) ByErrorCode(ctx context.Context, params WhatsappStatsByErrorCodeParams, opts ...option.RequestOption) (*WhatsAppStatsByErrorCodeResponse, error) {
	body, err := s.get(ctx, opts, func(ctx context.Context, cfg requestConfig) (*http.Response, error) {
		return s.client.oapi.GetWhatsAppStatsByErrorCode(ctx, params.toWire(), cfg...)
	})
	if err != nil {
		return nil, err
	}
	var out WhatsAppStatsByErrorCodeResponse
	if err := decodeBody(body, &out); err != nil {
		return nil, err
	}
	return &out, nil
}

// ByTemplate Outbound WhatsApp counts grouped by template, keyed by `template_id`, ranked by accepted volume. Resolve the id through the WhatsApp template endpoints for a name. Rows sum to less than the period's accepted total: a free-form send resolves no template, and some volume cannot be attributed to one at all, so take the denominator from `whatsapp.stats.summary`. Rows are the whole workspace's: the breakdown takes no dimension filter; to see one template over time, filter `whatsapp.stats.daily` by `template` instead. Cap the rows with `limit` (default 50, max 200).
func (s *WhatsappStatsService) ByTemplate(ctx context.Context, params WhatsappStatsByTemplateParams, opts ...option.RequestOption) (*WhatsAppStatsByTemplateResponse, error) {
	body, err := s.get(ctx, opts, func(ctx context.Context, cfg requestConfig) (*http.Response, error) {
		return s.client.oapi.GetWhatsAppStatsByTemplate(ctx, params.toWire(), cfg...)
	})
	if err != nil {
		return nil, err
	}
	var out WhatsAppStatsByTemplateResponse
	if err := decodeBody(body, &out); err != nil {
		return nil, err
	}
	return &out, nil
}

// ByTemplateCategory Outbound WhatsApp counts grouped by Meta's template category (`authentication`, `utility` and `marketing` today, and the vocabulary is open), ranked by accepted volume. Category is what WhatsApp prices a message on, so this is the breakdown that explains a spend change. A free-form send carries no category, so rows sum to less than the period's accepted total; take the denominator from `whatsapp.stats.summary`. Rows are the whole workspace's: the breakdown takes no dimension filter. Cap the rows with `limit` (default 50, max 200).
func (s *WhatsappStatsService) ByTemplateCategory(ctx context.Context, params WhatsappStatsByTemplateCategoryParams, opts ...option.RequestOption) (*WhatsAppStatsByTemplateCategoryResponse, error) {
	body, err := s.get(ctx, opts, func(ctx context.Context, cfg requestConfig) (*http.Response, error) {
		return s.client.oapi.GetWhatsAppStatsByTemplateCategory(ctx, params.toWire(), cfg...)
	})
	if err != nil {
		return nil, err
	}
	var out WhatsAppStatsByTemplateCategoryResponse
	if err := decodeBody(body, &out); err != nil {
		return nil, err
	}
	return &out, nil
}

// ByTag Outbound WhatsApp counts grouped by the tags a send attached, ranked by accepted volume. A row's tag is `name:value`, so one tag name spreads over as many rows as it has values. Rows are the whole workspace's: the breakdown takes no dimension filter; to see one tag over time, filter `whatsapp.stats.daily` by `tag` instead. Cap the rows with `limit` (default 50, max 200).
func (s *WhatsappStatsService) ByTag(ctx context.Context, params WhatsappStatsByTagParams, opts ...option.RequestOption) (*WhatsAppStatsByTagResponse, error) {
	body, err := s.get(ctx, opts, func(ctx context.Context, cfg requestConfig) (*http.Response, error) {
		return s.client.oapi.GetWhatsAppStatsByTag(ctx, params.toWire(), cfg...)
	})
	if err != nil {
		return nil, err
	}
	var out WhatsAppStatsByTagResponse
	if err := decodeBody(body, &out); err != nil {
		return nil, err
	}
	return &out, nil
}

// ByPhoneNumber Outbound WhatsApp counts grouped by the business number that sent them, ranked by accepted volume, each row marked as a number your workspace connected or one Bird manages on your behalf. Use it to find the sender behind a delivery problem: a row identifies the number in E.164 form. Rows are the whole workspace's: the breakdown takes no dimension filter.
func (s *WhatsappStatsService) ByPhoneNumber(ctx context.Context, params WhatsappStatsByPhoneNumberParams, opts ...option.RequestOption) (*WhatsAppStatsByPhoneNumberResponse, error) {
	body, err := s.get(ctx, opts, func(ctx context.Context, cfg requestConfig) (*http.Response, error) {
		return s.client.oapi.GetWhatsAppStatsByPhoneNumber(ctx, params.toWire(), cfg...)
	})
	if err != nil {
		return nil, err
	}
	var out WhatsAppStatsByPhoneNumberResponse
	if err := decodeBody(body, &out); err != nil {
		return nil, err
	}
	return &out, nil
}

// ByCountry Outbound WhatsApp delivery, read and latency stats grouped by the recipient's country, ranked by accepted volume. Use it to compare markets. A weak market cannot be narrowed further: no breakdown takes a country filter, so `whatsapp.stats.by_error_code` reports the workspace's failure reasons rather than the selected country's, and pairing the two is inference, not attribution. A recipient whose country cannot be resolved is counted under `ZZ`, so one-to-one sends always land somewhere. Group sends are the exception and are absent by design: one spans up to eight countries and has no single destination, so rows sum to `whatsapp.stats.summary` less that group volume. Do not report the difference as missing data. Rows are the whole workspace's: the breakdown takes no dimension filter.
func (s *WhatsappStatsService) ByCountry(ctx context.Context, params WhatsappStatsByCountryParams, opts ...option.RequestOption) (*WhatsAppStatsByCountryResponse, error) {
	body, err := s.get(ctx, opts, func(ctx context.Context, cfg requestConfig) (*http.Response, error) {
		return s.client.oapi.GetWhatsAppStatsByCountry(ctx, params.toWire(), cfg...)
	})
	if err != nil {
		return nil, err
	}
	var out WhatsAppStatsByCountryResponse
	if err := decodeBody(body, &out); err != nil {
		return nil, err
	}
	return &out, nil
}
