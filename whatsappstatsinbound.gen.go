// Code generated by surface-gen; DO NOT EDIT.
package bird

import (
	"context"
	"net/http"
	"time"

	"github.com/messagebird/bird-sdk-go/internal/oapi"
	"github.com/messagebird/bird-sdk-go/option"
)

// WhatsappStatsInboundSummaryParams filters the summary read.
type WhatsappStatsInboundSummaryParams struct {
	// Inclusive start of the window, either a calendar day (YYYY-MM-DD) or an RFC 3339 instant rounded down to the hour. The form you use selects the grain the total is resolved at. Interpreted in `timezone`, or in UTC when `timezone` is omitted. Must use the same form as `to`. A numeric UTC offset (for example `+05:45`) is rejected when `timezone` is set; pass a calendar day or a `Z` instant instead. Defaults to 30 days before `to` for day windows, or 168 hours before `to` for hour windows.
	From string
	// Inclusive end of the window, in the same form as `from`. Defaults to today, or the current hour for an hour window. A day window may not exceed 365 days and an hour window 720 hours.
	To string
	// IANA timezone identifier used to group statistics, for example `Asia/Kathmandu`. The default is UTC. Day and hour boundaries, including the default window when `from` and `to` are omitted, follow this timezone. When this parameter is set, pass `from` and `to` as calendar days or `Z` instants instead of timestamps with explicit UTC offsets.
	Timezone string
	// Set to `previous_period` to include the received-message count for the immediately preceding window of equal length. The response also includes the change between the two, so you can show "+X% vs last period" without a second request.
	Compare StatsComparePeriod
}

func (p WhatsappStatsInboundSummaryParams) toWire() *oapi.GetWhatsAppInboundStatsSummaryParams {
	return &oapi.GetWhatsAppInboundStatsSummaryParams{
		From:     optStr(p.From),
		To:       optStr(p.To),
		Timezone: optStr(p.Timezone),
		Compare:  optZero(p.Compare),
	}
}

// WhatsappStatsInboundDailyParams filters the daily read.
type WhatsappStatsInboundDailyParams struct {
	// Start date (inclusive), YYYY-MM-DD. Interpreted as a calendar day in `timezone` (a UTC day when `timezone` is omitted). The window may not exceed 365 days. Defaults to 30 days before `to` when omitted.
	From time.Time
	// End date (inclusive), YYYY-MM-DD. Interpreted as a calendar day in `timezone` (a UTC day when `timezone` is omitted). The window may not exceed 365 days. Defaults to today in that timezone when omitted.
	To time.Time
	// IANA timezone identifier used to group statistics, for example `Asia/Kathmandu`. The default is UTC. Day and hour boundaries, including the default window when `from` and `to` are omitted, follow this timezone. When this parameter is set, pass `from` and `to` as calendar days or `Z` instants instead of timestamps with explicit UTC offsets.
	Timezone string
}

func (p WhatsappStatsInboundDailyParams) toWire() *oapi.GetWhatsAppInboundStatsDailyParams {
	return &oapi.GetWhatsAppInboundStatsDailyParams{
		From:     optDate(p.From),
		To:       optDate(p.To),
		Timezone: optStr(p.Timezone),
	}
}

// WhatsappStatsInboundHourlyParams filters the hourly read.
type WhatsappStatsInboundHourlyParams struct {
	// Start of the window (RFC 3339 instant). Rounded down to the start of its hour (the local hour when `timezone` is set, otherwise the UTC hour), and that hour is included. The window may not exceed 30 days (720 hours). Defaults to 168 hours (7 days) before `to` when omitted.
	From time.Time
	// End of the window (RFC 3339 instant). Rounded down to the start of its hour (the local hour when `timezone` is set, otherwise the UTC hour), and that hour is included (both bounds inclusive). The window may not exceed 30 days (720 hours). Defaults to the current hour when omitted.
	To time.Time
	// IANA timezone identifier used to group statistics, for example `Asia/Kathmandu`. The default is UTC. Day and hour boundaries, including the default window when `from` and `to` are omitted, follow this timezone. When this parameter is set, pass `from` and `to` as calendar days or `Z` instants instead of timestamps with explicit UTC offsets.
	Timezone string
}

func (p WhatsappStatsInboundHourlyParams) toWire() *oapi.GetWhatsAppInboundStatsHourlyParams {
	return &oapi.GetWhatsAppInboundStatsHourlyParams{
		From:     optTime(p.From),
		To:       optTime(p.To),
		Timezone: optStr(p.Timezone),
	}
}

// WhatsappStatsInboundByPhoneNumberParams filters the by_phone_number read.
type WhatsappStatsInboundByPhoneNumberParams struct {
	// Inclusive start of the window, a calendar day (YYYY-MM-DD). Interpreted in `timezone`, or UTC when omitted. Must not be after `to`. Max window 365 days. Defaults to 30 days before `to` when omitted.
	From time.Time
	// Inclusive end of the window, a calendar day (YYYY-MM-DD). Interpreted in `timezone`, or UTC when omitted. Max window 365 days. Defaults to today in that timezone when omitted.
	To time.Time
	// IANA timezone identifier used to group statistics, for example `Asia/Kathmandu`. The default is UTC. Day and hour boundaries, including the default window when `from` and `to` are omitted, follow this timezone. When this parameter is set, pass `from` and `to` as calendar days or `Z` instants instead of timestamps with explicit UTC offsets.
	Timezone string
	// Maximum number of phone-number rows to return, ranked by received-message volume descending.
	Limit int
}

func (p WhatsappStatsInboundByPhoneNumberParams) toWire() *oapi.GetWhatsAppInboundStatsByPhoneNumberParams {
	return &oapi.GetWhatsAppInboundStatsByPhoneNumberParams{
		From:     optDate(p.From),
		To:       optDate(p.To),
		Timezone: optStr(p.Timezone),
		Limit:    optInt(p.Limit),
	}
}

// Summary Aggregate count of the WhatsApp messages your workspace received in one window, with the delta against the window immediately before when `compare` is set to `previous_period`. Inbound is counted separately from outbound: this answers "how much are customers writing to us", where `whatsapp.stats.summary` answers "how much are we sending".
func (s *WhatsappStatsInboundService) Summary(ctx context.Context, params WhatsappStatsInboundSummaryParams, opts ...option.RequestOption) (*WhatsAppInboundStatsSummaryResponse, error) {
	body, err := s.get(ctx, opts, func(ctx context.Context, cfg requestConfig) (*http.Response, error) {
		return s.client.oapi.GetWhatsAppInboundStatsSummary(ctx, params.toWire(), cfg...)
	})
	if err != nil {
		return nil, err
	}
	var out WhatsAppInboundStatsSummaryResponse
	if err := decodeBody(body, &out); err != nil {
		return nil, err
	}
	return &out, nil
}

// Daily Received WhatsApp message counts, one row per calendar day of the window, gap-filled so a silent day is a zero row. Days are calendar days in `timezone`, or UTC when it is omitted. Max window 365 days; `from` and `to` default to the trailing 30 days.
func (s *WhatsappStatsInboundService) Daily(ctx context.Context, params WhatsappStatsInboundDailyParams, opts ...option.RequestOption) (*WhatsAppInboundStatsResponse, error) {
	body, err := s.get(ctx, opts, func(ctx context.Context, cfg requestConfig) (*http.Response, error) {
		return s.client.oapi.GetWhatsAppInboundStatsDaily(ctx, params.toWire(), cfg...)
	})
	if err != nil {
		return nil, err
	}
	var out WhatsAppInboundStatsResponse
	if err := decodeBody(body, &out); err != nil {
		return nil, err
	}
	return &out, nil
}

// Hourly Received WhatsApp message counts, one row per hour, gap-filled. A window spans at most 720 hours (30 days); for anything longer use `whatsapp.stats.inbound.daily`. `from` and `to` are optional RFC 3339 instants.
func (s *WhatsappStatsInboundService) Hourly(ctx context.Context, params WhatsappStatsInboundHourlyParams, opts ...option.RequestOption) (*WhatsAppInboundStatsResponse, error) {
	body, err := s.get(ctx, opts, func(ctx context.Context, cfg requestConfig) (*http.Response, error) {
		return s.client.oapi.GetWhatsAppInboundStatsHourly(ctx, params.toWire(), cfg...)
	})
	if err != nil {
		return nil, err
	}
	var out WhatsAppInboundStatsResponse
	if err := decodeBody(body, &out); err != nil {
		return nil, err
	}
	return &out, nil
}

// ByPhoneNumber Received WhatsApp message counts grouped by the business number that received them, ranked by volume. Use it to see which of your numbers customers actually write to. Cap the rows with `limit` (default 50, max 200).
func (s *WhatsappStatsInboundService) ByPhoneNumber(ctx context.Context, params WhatsappStatsInboundByPhoneNumberParams, opts ...option.RequestOption) (*WhatsAppInboundStatsByPhoneNumberResponse, error) {
	body, err := s.get(ctx, opts, func(ctx context.Context, cfg requestConfig) (*http.Response, error) {
		return s.client.oapi.GetWhatsAppInboundStatsByPhoneNumber(ctx, params.toWire(), cfg...)
	})
	if err != nil {
		return nil, err
	}
	var out WhatsAppInboundStatsByPhoneNumberResponse
	if err := decodeBody(body, &out); err != nil {
		return nil, err
	}
	return &out, nil
}
