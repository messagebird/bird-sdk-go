// Code generated by surface-gen; DO NOT EDIT.
package bird

import (
	"context"
	"iter"
	"net/http"

	"github.com/messagebird/bird-sdk-go/internal/oapi"
	"github.com/messagebird/bird-sdk-go/option"
)

// EmailCompetitiveWatchlistBrandsCampaignsListParams filters the list. Zero-value fields are omitted.
type EmailCompetitiveWatchlistBrandsCampaignsListParams struct {
	// How many days back the response covers, counting from now. One of three fixed trend windows rather than an open date range, matching how a competitive-intelligence chart is read. Defaults to 30.
	Range int
	// Field to sort campaigns by.
	Sort EmailCompetitiveCampaignSort
	// Sort direction. Defaults to `desc`, which sorts from newest to oldest or largest to smallest, depending on the selected sort field.
	Order SortOrder
	// Maximum number of items to return per page.
	Limit int
	// Cursor from the `prev_cursor` or `refresh_cursor` field of a previous list response. Returns items immediately before the cursor position in the current sort order. `prev_cursor` returns the preceding page. `refresh_cursor` anchors at the first row of that response, which on a newest-first sort is how to fetch the items that have appeared since.
	EndingBefore string
}

func (p EmailCompetitiveWatchlistBrandsCampaignsListParams) toWire(startingAfter string) *oapi.GetEmailCompetitiveBrandCampaignsParams {
	return &oapi.GetEmailCompetitiveBrandCampaignsParams{
		Range:         optInt(oapi.GetEmailCompetitiveBrandCampaignsParamsRange(p.Range)),
		Sort:          optZero(p.Sort),
		Order:         optZero(p.Order),
		Limit:         optInt(p.Limit),
		EndingBefore:  optStr(p.EndingBefore),
		StartingAfter: optStr(startingAfter),
	}
}

// ListPage fetches one page of results. Pass the previous page's NextCursor as
// startingAfter to advance; "" starts from the first page.
func (s *EmailCompetitiveWatchlistBrandsCampaignsService) ListPage(ctx context.Context, watchlistBrandId string, params EmailCompetitiveWatchlistBrandsCampaignsListParams, startingAfter string, opts ...option.RequestOption) (*EmailCompetitiveCampaignFeed, error) {
	body, err := s.get(ctx, opts, func(ctx context.Context, cfg requestConfig) (*http.Response, error) {
		return s.client.oapi.GetEmailCompetitiveBrandCampaigns(ctx, oapi.CompetitiveWatchlistBrandID(watchlistBrandId), params.toWire(startingAfter), cfg...)
	})
	if err != nil {
		return nil, err
	}
	var out EmailCompetitiveCampaignFeed
	if err := decodeBody(body, &out); err != nil {
		return nil, err
	}
	return &out, nil
}

// List Takes a watchlist_brand_id from `email.competitive.watchlist.get`. Returns a cursor page, default 25 rows and maximum 100. Cursors retain the brand, range and ordering: next_cursor advances with starting_after; prev_cursor or refresh_cursor reads preceding rows with ending_before. captured and promo_rate describe a sample independently of this page. next_cursor controls continuation; truncated can also describe the sample. API-key calls require Insights preview access for your organization.
// Range over it; the second value is non-nil only on the iteration where a
// fetch failed.
func (s *EmailCompetitiveWatchlistBrandsCampaignsService) List(ctx context.Context, watchlistBrandId string, params EmailCompetitiveWatchlistBrandsCampaignsListParams, opts ...option.RequestOption) iter.Seq2[*EmailCompetitiveCampaign, error] {
	return paginate(func(cursor string) ([]EmailCompetitiveCampaign, *string, error) {
		pageParams := params
		if cursor != "" {
			pageParams.EndingBefore = ""
		}
		page, err := s.ListPage(ctx, watchlistBrandId, pageParams, cursor, opts...)
		if err != nil {
			return nil, nil, err
		}
		if page.Data == nil {
			return nil, page.NextCursor, nil
		}
		return *page.Data, page.NextCursor, nil
	})
}

// Get Takes a watchlist_brand_id and a campaign_id from `email.competitive.watchlist.brands.campaigns.list`. campaign_id is text to preserve its digits. Returns the campaign's figures and nullable creative URL; a campaign outside that watched brand returns not found. API-key calls require Insights preview access for your organization.
func (s *EmailCompetitiveWatchlistBrandsCampaignsService) Get(ctx context.Context, watchlistBrandId string, campaignId string, opts ...option.RequestOption) (*EmailCompetitiveCampaign, error) {
	body, err := s.get(ctx, opts, func(ctx context.Context, cfg requestConfig) (*http.Response, error) {
		return s.client.oapi.GetEmailCompetitiveBrandCampaign(ctx, oapi.CompetitiveWatchlistBrandID(watchlistBrandId), campaignId, cfg...)
	})
	if err != nil {
		return nil, err
	}
	var out EmailCompetitiveCampaign
	if err := decodeBody(body, &out); err != nil {
		return nil, err
	}
	return &out, nil
}
